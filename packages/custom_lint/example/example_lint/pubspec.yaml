name: custom_lint_example_lint
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^10.0.1
  analyzer_plugin: ^0.14.1
  custom_lint_builder:
    path: ../../../custom_lint_builder

dev_dependencies:
  custom_lint:
